server:
  port: 8080

spring:
  datasource:
    username: root
    password: ieroglif47
    url: jdbc:mysql://localhost:3306/search_engine7?useSSL=false&requireSSL=false&allowPublicKeyRetrieval=true
  jpa:
    properties:
      hibernate:
    dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: validate
    show-sql: true
# example values

indexing-settings:
  sites:
    - url: https://lenta.ru
      name: Лента.ру
#    - url: https://skillbox.ru
#      name: Skillbox
    - url: https://www.playback.ru
      name: PlayBack.Ru
